# -*- mode: snippet -*-
# name: Insert an interface
# key: intf
# expand-env: ((yas-indent-line 'fixed) (yas-wrap-around-region 't))
# --
interface ${1:`(car (split-string (buffer-name) "\\.php"))`}
{
    $0
}